# functions/.env.staging

# Ganti dengan URL hosting Firebase untuk proyek staging Anda
# Biasanya: https://<staging-project-id>.web.app
APP_URL=https://project-pulazzz-staging-5b316.web.app

# Konfigurasi email untuk lingkungan staging
# Anda bisa menggunakan alamat email tes atau sandbox
EMAIL_TO=staging-test@example.com
EMAIL_FROM=Staging Order System <noreply@your-staging-domain.com>

# Catatan: Kunci API SendGrid (SENDGRID_API_KEY) harus diatur sebagai
# variabel lingkungan fungsi di Firebase.
# Gunakan perintah:
# firebase functions:config:set sendgrid.key="YOUR_STAGING_KEY" -P project-pulazzz-staging-5b316
